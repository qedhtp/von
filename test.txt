>/ ˈæp(ə)l /</span><div class="phraseSpeech phonetic-speech" data-v-7911b552 data-v-39fab836><a title="点击发音" href="javascript:;" class="pronounce" data-v-7911b552></a></div></div><div class="per-phone" data-v-39fab836><span data-v-39fab836>美</span><span class="phonetic" data-v-39fab836>/ ˈæp(ə)l /<



// Copy the response body to the file and also to a variable
body, err := io.ReadAll(response.Body)
if err !=nil{
    log.Fatal(err)
}
body_string := string(body)
//regex match
pattern := `>/ .+ /<`
re := regexp.MustCompile(pattern)
matches := re.FindAllString(body_string, -1)


fmt.Println(matches)
var firstMatch string
// var secondMatch string
if len(matches) == 1 {
    firstMatch = matches[0]
    //secondMatch = matches[1]

    if len(firstMatch) > 0 {
        //Print the first match
        fmt.Println(firstMatch)
    } else{
        fmt.Println("no match")
    }
}


"github.com/PuerkitoBio/goquery"